; Test: Ambiguity blocking (no superiority tie-breaking)
; Coverage: conflicting defeasible rules with NO superiority
; SDL semantics: NEITHER conclusion is derivable (ambiguity blocking)

(given quaker)
(given republican)
(normally r1 quaker pacifist)
(normally r2 republican (not pacifist))

; No superiority => ambiguity blocks both conclusions
; EXPECT: +D quaker
; EXPECT: +d quaker
; EXPECT: +D republican
; EXPECT: +d republican
; EXPECT: -d pacifist
; EXPECT: -d ~pacifist
