; Test: Conflicting facts
; Coverage: traditional DL subsumes every +D proof into +d.

(given p)
(given (not p))

; Both p and ~p are definitely provable (inconsistent theory).
; Definite subsumption is unconditional, including inconsistent inputs.
; EXPECT: +D p
; EXPECT: +D ~p
; EXPECT: +d p
; EXPECT: +d ~p
